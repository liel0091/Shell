import React from 'react';
import { Wrapper, Grid, Section } from './footer.styled';

export const Footer = () => {
    return (
        <Wrapper>
            <Grid>
                <Section>Shell App</Section>
                <Section>MIT</Section>
                <Section>liel0091</Section>
            </Grid>
        </Wrapper>
    );
};
